{
  "dependencies": {
    "gh-pages": "^6.3.0",
    "ml5": "^1.0.1",
    "p5": "^2.2.3",
    "vite": "^8.0.8"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  }
}
